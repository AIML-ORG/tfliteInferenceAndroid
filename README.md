This repo has sample android code for using DJLtokenizer as well as code for inferencing using TFLite model.
Place the model.tflite as well as the tokenizer.json files inside the assets folder of the project.
Compile and build the project to run the sample app.
